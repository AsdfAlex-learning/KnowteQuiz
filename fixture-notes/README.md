# Fixture Notes

This directory contains sample Markdown notes for smoke testing KnowteQuiz.

Point the app to this directory to verify:
- Directory scanning (should find .md files in subdirectories)
- Note reading and rendering (Markdown + code blocks)
- Quiz generation (with a configured LLM)
